<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GetirBootcampProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/open_sans</item>
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="cardBackgroundColor">@color/white</item>
        <item name="colorOnBackground">@color/bg_primary_subtle</item>

    </style>

    <style name="Theme.GetirBootcampProject" parent="Base.Theme.GetirBootcampProject" />
</resources>